# Launchpad core (reference)

Frozen copy of `genai-launchpad-main/app/launchpad/core` for the P2P workflow engine.

Runtime code lives in `app/workflow/core/` and is adapted to `ProcessingContext`.
Do not import `reference.launchpad` from application code.
